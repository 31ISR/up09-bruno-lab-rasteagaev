info:
  name: info_user
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/auth/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

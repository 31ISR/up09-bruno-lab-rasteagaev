info:
  name: creat_amount
  type: http
  seq: 9

http:
  method: POST
  url: "{{base_url}}/api/items/:id/bids"
  params:
    - name: id
      value: "1773206487684"
      type: path
  body:
    type: json
    data: |-
      {
        "amount": 85000
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

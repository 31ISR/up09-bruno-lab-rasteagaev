info:
  name: delete_items
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base_url}}/api/items/:id"
  params:
    - name: id
      value: "1773220552672"
      type: path
  body:
    type: json
    data: |-
      {
        "success": true,
        "message": "Item deleted"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
